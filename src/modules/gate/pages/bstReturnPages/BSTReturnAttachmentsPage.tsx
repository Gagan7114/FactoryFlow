import { BST_RETURN_FLOW } from '../../constants/entryFlowConfig';
import SharedAttachmentsPage from '../shared/SharedAttachmentsPage';

export default function BSTReturnAttachmentsPage() {
  return <SharedAttachmentsPage config={BST_RETURN_FLOW} />;
}
