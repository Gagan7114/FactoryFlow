import { BST_IN_FLOW } from '../../constants/entryFlowConfig';
import SharedAttachmentsPage from '../shared/SharedAttachmentsPage';

export default function BSTInAttachmentsPage() {
  return <SharedAttachmentsPage config={BST_IN_FLOW} />;
}
